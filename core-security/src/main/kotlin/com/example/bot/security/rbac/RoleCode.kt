package com.example.bot.security.rbac

/**
 * Enumeration of all supported roles in the system.
 */
enum class RoleCode {
    OWNER,
    GLOBAL_ADMIN,
    HEAD_MANAGER,
    CLUB_ADMIN,
    MANAGER,
    ENTRY_MANAGER,
    PROMOTER,
    GUEST,
}
